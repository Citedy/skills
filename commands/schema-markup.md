---
description: Add, fix, or optimize schema markup (JSON-LD structured data)
---
Treat `$1` as the page URL or page type to add schema markup for.
If `$1` is missing, ask the user what page they want schema markup for and stop.

Read `.claude/skills/schema-markup/SKILL.md` and follow the workflow to implement schema markup.

Present the generated JSON-LD code and placement instructions.
